# 🐳 Intro to DB with Docker + SQLite

This project sets up a cross-platform Dockerized environment for running SQLite database exercises.

---

## 📦 Prerequisites

- **Install Docker Desktop**  
  - Mac users: check for compatibility (Apple Silicon vs Intel)
  - [Download Docker Desktop](https://www.docker.com/products/docker-desktop/)
  - Make sure Docker is **running** before you begin

---

## 🐧 Linux / WSL Setup

1. Open a terminal window
2. Navigate to the folder containing this `readme.txt` file
3. Run the following:

```bash
./_setup-linux.bash
./docker/build
./docker/run
